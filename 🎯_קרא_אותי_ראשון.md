# 🎯 קרא אותי ראשון!

## היי Team Member B! ברוך הבא למערכת שלך 👋

בניתי לך **מערכת מלאה** לחלק שלך בפרויקט. הכל מוכן, רק צריך להעלות ל-RunPod!

---

## 📚 איזה קובץ לקרוא?

### אם אתה רוצה להתחיל **מיד** (90 דקות):
👉 **`STEP_BY_STEP.md`** - צעד אחר צעד מאפס עד RunPod

### אם אתה רוצה להבין **מה בניתי**:
👉 **`PROJECT_SUMMARY.md`** - סקירה מלאה של כל הקבצים

### אם אתה רוצה **Quick Start**:
👉 **`GET_STARTED.md`** - תרחישי התחלה מהירה

### אם אתה רוצה **להבין את הארכיטקטורה**:
👉 **`ARCHITECTURE.md`** - דיאגרמות ותיאור מפורט

---

## 🚀 ה-Quick Quick Start (לממהרים)

```powershell
# 1. Setup (3 דקות)
cd c:\dev\intelligent_semaphore
.\scripts\setup_windows.ps1

# 2. Git (3 דקות)
git add .
git commit -m "Team B setup"
# צור repo ב-github.com
git remote add origin https://github.com/[YOU]/intelligent-semaphore.git
git push -u origin main

# 3. RunPod (5 דקות)
# לך ל-runpod.io → Deploy → RTX 3090
# Ports: 2000, 8000, 6080
# Deploy!

# 4. ב-RunPod Terminal (35 דקות)
git clone https://github.com/[YOU]/intelligent-semaphore.git
cd intelligent-semaphore
bash scripts/setup_runpod.sh  # המתן 30 דקות
docker run -d --name carla-system --gpus all \
  -p 2000:2000 -p 8000:8000 -p 6080:6080 \
  carla-vision-system:latest

# 5. גש ב-דפדפן
# noVNC: https://[pod-id]-6080.proxy.runpod.net
# API: https://[pod-id]-8000.proxy.runpod.net/docs
```

**זהו! תראה את CARLA רצה!** 🎉

---

## 📁 מה יש כאן?

```
59 קבצים מאורגנים:

📖 מדריכים (10):
   START_HERE.md              ← התחל כאן
   GET_STARTED.md             ← מדריך מהיר
   STEP_BY_STEP.md           ← צעד אחר צעד
   PROJECT_SUMMARY.md         ← סקירת הפרויקט
   ARCHITECTURE.md            ← ארכיטקטורה
   TEAM_A_INTEGRATION.md      ← מדריך ל-Team A
   + עוד...

💻 קוד (25):
   carla_integration/         ← חיבור ל-CARLA
   yolo_detection/            ← זיהוי רכבים
   sensing_pipeline/          ← בניית observations
   api/                       ← REST API
   + tests, examples

⚙️ קונפיגורציה (10):
   config/*.yaml              ← הגדרות מערכת
   docker/                    ← Docker files
   requirements.txt           ← Dependencies

🛠️ כלים (8):
   scripts/                   ← Scripts עזר
   tests/                     ← בדיקות
```

---

## 🎯 המטרה שלך

אתה אחראי על:
1. ✅ **CARLA** - סימולציה על GPU
2. ✅ **YOLO** - זיהוי רכבים
3. ✅ **ROI Mapping** - המרה לספירת נתיבים
4. ✅ **API** - חיבור ל-Team A

**הכל כבר בנוי!** רק צריך להעלות ל-RunPod.

---

## ✉️ מה לתת ל-Team A

כשתסיים setup, תן לו:
1. ה-API URL שלך
2. הקובץ `TEAM_A_INTEGRATION.md`
3. הקובץ `docs/API_SPEC.md`

**הוא יוכל להתחיל מיד לאמן PPO!**

---

## 🎓 מסלולי למידה

### מסלול מהיר (מומלץ להתחלה):
1. `STEP_BY_STEP.md` (קרא + עשה)
2. Deploy ל-RunPod
3. ✅ סיימת!

### מסלול מעמיק:
1. `START_HERE.md` (5 דק')
2. `PROJECT_SUMMARY.md` (5 דק')
3. `ARCHITECTURE.md` (10 דק')
4. `docs/RUNPOD_SETUP.md` (10 דק')
5. `STEP_BY_STEP.md` (עשה)
6. ✅ סיימת!

### מסלול "אני רוצה להבין קוד":
1. קרא כל ה-.py files
2. הרץ `python scripts/check_config.py`
3. הבן את הזרימה
4. אז Deploy

---

## 💡 Tips חשובים

### 💰 כסף
- **Stop Pod** כשלא עובד = חיסכון!
- RTX 3090 = $0.34/שעה
- 10 שעות עבודה = $3.40

### 🐛 Debugging
- תמיד בדוק `docker logs carla-system`
- השתמש ב-camera stream לראות מה קורה
- הרץ `scripts/test_system.py`

### 🤝 תקשורת
- עדכן את Team A על שינויים
- תאם לפני downtime
- תעדכן URLs כשמשנים Pod

---

## ❓ שאלות נפוצות

**ש: כמה זמן זה לוקח?**  
ת: 90 דקות (47 דקות עבודה + 40 דקות המתנה לבנייה)

**ש: כמה זה עולה?**  
ת: ~$0.35 ל-setup, אחר כך $0.34/שעה שהמערכת רצה

**ש: מה אם משהו לא עובד?**  
ת: `docs/TROUBLESHOOTING.md` + `docker logs carla-system`

**ש: האם אני צריך להתקין CARLA על המחשב שלי?**  
ת: **לא!** הכל רץ ב-RunPod. אתה רק רואה דרך הדפדפן.

**ש: מתי אני נותן את זה ל-Team A?**  
ת: אחרי שעברת את שלב 9 ב-STEP_BY_STEP.md

---

## 🎊 מוכן להתחיל?

### העבר ל:
**→ `STEP_BY_STEP.md` ←**

**בהצלחה! אתה הולך לעשות דברים מדהימים! 🚦✨**

---

*P.S. - אם תצליח להריץ את זה ולראות CARLA ב-noVNC, תקבל תחושת הצלחה מדהימה! זה באמת עובד! 🎉*
